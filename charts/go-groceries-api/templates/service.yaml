apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-groceries-api.fullname" . }}
  labels:
    {{- include "go-groceries-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-groceries-api.selectorLabels" . | nindent 4 }}
